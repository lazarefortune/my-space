{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-success my-2"> <i class="fas fa-check"></i> {{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
