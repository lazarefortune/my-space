{% extends 'layouts/dashboard.html.twig' %}

{% block admin_title %}Edit School{% endblock %}

{% block admin_content %}
    <h1>Edit School</h1>

    {{ include('admin/school/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_school_index') }}" class="btn btn-sm btn-secondary my-2">back to list</a>

    {{ include('admin/school/_delete_form.html.twig') }}
{% endblock %}
