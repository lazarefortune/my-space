{% extends 'layouts/admin.html.twig' %}

{% block title %}Edit MailSend
{% endblock %}

{% block site_content %}
	
    <div class="row">
        <div class="col-md-7 mx-auto">
            <h1>Edit MailSend</h1>

	{# {{ include('admin_mail_send/_form.html.twig', {'button_label': 'Update'}) }} #}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}

	{# <a href="{{ path('admin_mail_send_index') }}">back to list</a> #}

	{# {{ include('admin_mail_send/_delete_form.html.twig') }} #}
    <form method="post" action="{{ path('admin_mail_send_delete', {'id': mail_send.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mail_send.id) }}">
    <button class="btn">Delete</button>
</form>
        </div>
    </div>

{% endblock %}
