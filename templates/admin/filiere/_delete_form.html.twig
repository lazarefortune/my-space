<form method="post" action="{{ path('admin_filiere_delete', {'id': filiere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ filiere.id) }}">
    <button class="btn btn-sm btn-outline-danger my-2"> <i class="fas fa-trash"></i> Delete</button>
</form>
