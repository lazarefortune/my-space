{% extends "layouts/admin.html.twig" %}

{% block site_content %}

	<div class="row mt-4">
		<div class="mx-auto col-md-12">

			{% include 'layouts/flash-messages.html.twig' %}

			{# <div class="widget-content widget-content-area mb-3">
							<div class="d-flex justify-content-between">
								<h3 class="">Profile</h3>
								<a href=" {{ path('account_edit') }} " class="mt-2 edit-profile">
									<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewbox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-edit-3">
										<path d="M12 20h9"></path>
										<path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"></path>
									</svg>
								</a>
							</div>
							<div class="text-center user-info">
								<img src="assets/img/90x90.jpg" alt="avatar">
								<p class=""> {{ app.user.nom }} {{ app.user.prenom }} </p>
							</div>
							<div class="user-info-list">
			
								<div class="">
									
								</div>
							</div>
						</div> #}

			<div class="statbox widget box box-shadow mb-5">
				{# <h6 class="card-header font-weight-bold bg-dark">
																									Profil
																								</h6> #}
				<!-- .card-body -->

					<div class="widget-content widget-content-area"> <div
						class="mb-3 d-flex justify-content-between">
						{# <div class="row">
																		<div class="col-xl-12 col-md-12 col-sm-12 col-12">
																		</div>
																	</div> #}
						<h4>Profil</h4>
						<a href=" {{ path('account_edit') }} " class="mt-2 edit-profile">
							<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewbox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-edit-3">
								<path d="M12 20h9"></path>
								<path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"></path>
							</svg>
						</a>
					</div>

					<!-- form row -->
					<div
						class="form-row">
						<!-- form column -->
						<div class="col-md-6 mb-3">
							<label for="form_nomEval">Nom
								<span class="text-danger">*</span>
							</label>
							<input type="text" name="" disabled class="form-control" value=" {{ app.user.nom }} " id="">
						</div>
						<!-- /form column -->
						<!-- form column -->
						<div class="col-md-6 mb-3">
							<label for="input02">Prénom
								<span class="text-danger">*</span>
							</label>
							<input type="text" name="" disabled class="form-control" value=" {{ app.user.prenom }} " id="">

						</div>
						<!-- /form column -->
					</div>
					<!-- /form row -->

					<div class="form-row">
						<div
							class="col-md-6 mb-3">
							<!-- .form-group -->
							<div class="form-group">
								<label for="input03">Adresse e-mail
									<span class="text-danger">*</span>
								</label>
								<input type="email" name="" class="form-control" value=" {{ app.user.email }} " disabled id="">
								<small class="text-{{ app.user.isVerified == 1 ? 'success' : 'danger'}}">
									{{ app.user.isVerified == 1 ? '<i data-feather="check-circle"></i>Adresse mail vérifiée' : '<i data-feather="x-circle"></i>Adresse mail non vérifié. <a href="" class="text-primary">Vérifier maintenant</a>' }}
									<a href=" {{ path('send_verify_email') }} " class="text-primary">Vérifier maintenant</a>
								</small>
							</div>
						</div>
						<div
							class="col-md-6 mb-3">
							<!-- .form-group -->
							<div class="form-group">
								<label for="input03">Adresse e-mail secondaire</label>
								<input type="email" class="form-control" value=" {{ app.user.secondEmail }} " disabled name="" id="">
							</div>
							<!-- /.form-group -->
						</div>
					</div>

					
					<a href=" {{ path('delete_account', { 'id': app.user.idUser }) }} " class="btn btn-danger">Supprimer mon compte</a>
					
					<!-- /.form-group -->
				<!-- .form-group -->
					{# <div class="form-group">
											<label for="identifiant">Identifiant de connexion</label>
											<input type="text" name="" class="form-control" value=" {{ app.user.login }} " disabled id="">
											<div class=" mt-1">
												<span class="badge badge-primary">
													<small id="sh-text4" class="form-text mt-0">Cet identifiant est unique et vous permet de vous connecter à la plateforme.</small>
												</span>
											</div>
										</div> #}
					{# <small class="text-muted">Cet identifiant est unique et vous permet de vous connecter à la plateforme.</small> #}
					<!-- /.form-group -->
					<!-- .form-group -->
					{# <div class="form-group">
											<label for="newPassword">Nouveau mot de passe</label>
											<input type="password" class="form-control" name="newPassword" id="newPassword">
											<small>Uniquement si vous Souhaitez modifier votre mot de passe, sinon laissez vide.</small>
										</div> #}
					<!-- /.form-group -->
					{# <hr> #}
					<!-- .form-actions -->
					<!-- enable submit btn when user type their current password -->
					{# <div class="form-group"> <label for="oldPassword">Mot de passe actuel *</label>
												<input
												type="password" class="form-control mr-3" name="oldPassword" id="oldPassword" placeholder="Entrez votre mot de passe actuel" required="">
											</div> #}
					{# <small>Obligatoire pour mettre à jour vos données.</small> #}
					{# <button type="submit" class="btn btn-primary text-nowrap ml-auto" id="send" disabled>Mettre à jour</button> #}
					<!-- /.form-actions -->


					<!-- /form -->
				</div>
				<!-- /.card-body -->
			</div>
			<!-- /.card -->
		</div>
	</div>


	<div>

	{% endblock %}

	{% block javascripts %}
		<script>
			document.getElementById("oldPassword").addEventListener("keyup", function () {
var nameInput = document.getElementById('oldPassword').value;
if (nameInput != "") {
document.getElementById('send').removeAttribute("disabled");
} else {
document.getElementById('send').setAttribute("disabled", null);
}
});
		</script>
	{% endblock %}
