{% extends 'layouts/dashboard.html.twig' %}

{% block admin_title %}Matiere{% endblock %}

{% block admin_content %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ matiere.name }}</td>
            </tr>
            <tr>
                <th>Coeff</th>
                <td>{{ matiere.coeff }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_matiere_index') }}">back to list</a>

    <a href="{{ path('admin_module_show', {'id' : matiere.module.id}) }}" class="btn btn-sm btn-secondary my-2">back to module</a>

    <a href="{{ path('admin_matiere_edit', {'id': matiere.id}) }}">edit</a>

    {{ include('admin/matiere/_delete_form.html.twig') }}
{% endblock %}
